"use client";

/**
 * UX-012 Slice B — Sheet is the Drawer family alias (same focus-trap chrome).
 * Prefer Drawer API; Sheet re-exports for component-standards naming.
 */
export { Drawer as Sheet, type DrawerProps as SheetProps } from "./drawer";
