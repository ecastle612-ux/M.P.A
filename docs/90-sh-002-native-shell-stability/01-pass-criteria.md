# 01 — Pass Criteria

PASS only if all are true under workflow + matrix testing:

| Gate | Requirement |
| --- | --- |
| D1 | Drawer never visibly rebuilds |
| D2 | Navigation remains visually stable |
| H1 | Header never shifts |
| L1 | Logo never changes unexpectedly |
| A1 | Floating AI never causes shell rerenders / CLS |
| F1 | Input fields never lose focus while typing |
| K1 | Keyboard never dismisses unexpectedly |
| Y1 | No hydration flicker |
| C1 | No layout shifts in shell chrome |
| M1 | Mobile interaction feels native |
| W1 | PMs can work continuously without UI interruptions |

**Blocked until PASS:** UX-009 Units → portals.
